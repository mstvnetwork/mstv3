// Function to navigate to channel category pages
function navigateTo(category) {
  // You can either open a new page or load content dynamically
  // For now, redirecting to a separate HTML file
  window.location.href = `pages/${category}.html`;
}

// You can later enhance this to dynamically load logos and videos using AJAX or fetch API
